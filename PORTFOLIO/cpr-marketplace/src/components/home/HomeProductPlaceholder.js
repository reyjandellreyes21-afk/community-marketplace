/** Empty slot matching product card footprint — no labels or imagery. */
export default function HomeProductPlaceholder() {
  return (
    <div
      className="min-h-[280px] rounded-2xl border border-dashed border-slate-200 bg-white"
      aria-hidden
    />
  );
}
