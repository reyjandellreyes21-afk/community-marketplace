# Loaded automatically for `npm run build:capacitor` (vite build --mode capacitor).
VITE_CAPACITOR=true

# Native builds cannot use the Vite dev proxy — set your deployed API origin, e.g.:
# VITE_API_URL=https://your-api.example.com/api/v1
